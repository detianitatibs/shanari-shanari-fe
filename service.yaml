apiVersion: v1
kind: Service
metadata:
  name: shanari-shanari-fe-service
spec:
  selector:
    app: shanari-shanari-fe
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort
