import CardProps from "./CardProps";

interface ContentsListProps {
  subject_name: string;
  abbreviation_name: string;
  cards: CardProps[];
  is_button: boolean;
}

export default ContentsListProps;
